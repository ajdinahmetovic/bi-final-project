version: "3.8"

services:
  superset:
    build: .
    container_name: superset
    ports:
      - "8088:8088"
    environment:
      SUPERSET_SECRET_KEY: "thisISaSECRET_1234"
    volumes:
      - superset_home:/app/superset_home

volumes:
  superset_home:
